// I am importing the Packt.Shared type.
using Packt.Shared;

// I am writing in public acess modifier encapsulation a class named BanckAccount
public class BankAccount
{

    // I am writing  in public acess modifier encapsulation mode a instance field member with string type nullable named AccountName in pascal/title.
    public string? AccountName;

    // I am writing in public acess modifier encapsulation mode a instance field member with decimal type named Balance.
    public decimal? Balance;

    // I am writing in public acess modifier encapsulation membera field with static mode and decimal type named InterestRate;
    public static decimal? InterestRate;
    

}